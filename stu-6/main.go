package main

// go + chan + close(chan) + 单项信道 + 模拟锁
// select + case + default
// sync.Mutex + sync.RWMutex + sync.WaitGroup + sync.Once + sync.Map + atomic + sync.Pool
// context.Context
// time.Timer + time.Ticker
// 协程池
func main() {
	test1()
	test2()
	test3()
	test4()
	test5()
	test6()
}
